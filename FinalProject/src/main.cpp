#include "Game.h"
using namespace std;

int main() {
    // The entry point is clean. 
    // It simply instantiates the Game engine and starts the loop.
    Game game;
    game.run();

    return 0;
}